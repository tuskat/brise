{
  "name": "brise",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:verbose": "vitest run --reporter=verbose"
  },
  "dependencies": {
    "@astrojs/node": "^10.0.5",
    "astro": "^6.1.4",
    "better-sqlite3": "^12.9.0"
  },
  "devDependencies": {
    "vitest": "^4.1.5"
  }
}
